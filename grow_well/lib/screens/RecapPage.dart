import 'package:flutter/material.dart';

class RecapPageWidget extends StatelessWidget {
  const RecapPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Pagina Recap',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}